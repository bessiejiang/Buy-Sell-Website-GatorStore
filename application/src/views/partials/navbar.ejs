<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">TEAM 12 </a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarNavAltMarkup"
    aria-controls="navbarNavAltMarkup"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav w-100 justify-content-center">
      
        <a class="nav-item nav-link" href="#">About </a>
        <a class="nav-item nav-link mr-auto" href="#">Sell</a>

      <form class="w-50" action="/getItems" method="get" id="search-form">
        <div class="input-group">
          <select class="custom-select btn" name="category">
            <option value="">All</option>
            <% for (let category of categories) { %>
            <option value="<%= category.id %>"><%= category.name %></option>
            <% } %>
          </select>

          <input
            type="text"
            class="form-control form-inline w-75"
            placeholder="Go Find It!"
            name="search"
          />
          <button
            class="btn btn-warning input-group-append"
            type="submit"
            id="button-addon2"
          >
            Search
          </button>
        </div>
      </form>

      <a class="nav-item nav-link ml-auto" href="#">Sign Up</a>
      <a class="nav-item nav-link" href="#">Login</a>
    </div>
  </div>
</nav>
