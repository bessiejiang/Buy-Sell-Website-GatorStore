#!/usr/bin/env bash

vagrant status | grep 'default\s*running' -q || vagrant up
vagrant ssh -c 'cd /vagrant && /usr/bin/env bash'

echo ''
echo -e "Don't forget to run '\e[33mvagrant suspend\e[39m' when done working"
